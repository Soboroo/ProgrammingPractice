#include <stdio.h>
#include <string.h>

int solution(const char *skill, const char *skill_trees[], size_t skill_trees_len)
{
}

int main()
{
	const char *tree[] = {"BACDE", "CBADF", "AECB", "BDA"};
	printf("%d\n", solution("CBD", tree, sizeof(tree)));
}