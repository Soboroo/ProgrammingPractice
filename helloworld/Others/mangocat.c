#include <stdio.h>

int main(){
    int i, a, b, k, q;
    char m[10001], s[10001];
    gets(m);
    gets(s);
    scanf("%d %d", &k, &q);
    for (i = 0; i < q; i++){
        scanf("%d %d", &a, &b);
    }
}

char mango(int k, char m[], char s[]){
    
}
