#include <iostream>
#include <vector>
using namespace std;

int main() {
  int n;
  cin >> n;
  for (int i = 0; i < n; i++) {
    int m, t;
    cin >> m;
    vector<int> v;
    for (int j = 0; j < m; j++) {
      cin >> t;
      v.push_back(t);
    }
    cin >> m;
    for (int j = 0; j < m; j++) {
    }
  }
}