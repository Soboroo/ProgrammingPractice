#include <iostream>
#include <vector>
using namespace std;

void bfs(vector<vector<int>> *miro, int x, int y){
	
}

int main() {
  int n, col;
  cin >> n;
  for (int i = 0; i < n; i++) {
    cin >> col;
    vector<vector<int>> miro(2, vector<int>(col));
    vector<vector<int>> visited(2, vector<int>(col, 0));
  }
}